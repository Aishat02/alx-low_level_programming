#include "main.h"

/**
 * _islower - checks for lowercase character.
 *
 * Return: Always 0.
 */
int _islower(int c)
{

	if (c == _islower ('R'))
	{
		_putchar(c + '0');
	}
	else
	{
		_putchar(c + '1');
	}
	_putchar('\n');
	return (c);
}
